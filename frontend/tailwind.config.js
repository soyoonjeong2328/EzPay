/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}"  // ✨ React 컴포넌트에서 Tailwind 적용 가능하도록 설정
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  